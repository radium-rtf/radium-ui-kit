import {createUseStyles} from 'react-jss'

export const useStyles = createUseStyles({
  card: {
    display: 'flex',
    flexDirection: 'column',
    gap: '1rem',
    borderRadius: '1.5rem',
    backgroundColor: '#fff',
    padding: '1.5rem',
  },
})
